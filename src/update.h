#ifndef UPDATE_H
#define UPDATE_H

#include "util/util.h"

#define PATH "../res"

//static void dirwalk(char *dir, void (*fcn)(char *));
//static void fname(char *name);
void updateDir(char *name);

#endif
